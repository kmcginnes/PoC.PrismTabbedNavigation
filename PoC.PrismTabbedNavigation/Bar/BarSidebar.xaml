<UserControl x:Class="PoC.PrismTabbedNavigation.Bar.BarSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border BorderThickness="1" BorderBrush="Cyan">
            <HeaderedItemsControl Header="Bar" Style="{StaticResource SidebarHeaderedItemsControl}">
                <RadioButton GroupName="SidebarGroup" Content="Show Bar" Command="{Binding ShowBar}"/>
                <RadioButton GroupName="SidebarGroup" Content="Show Bar" Command="{Binding ShowBar}"/>
            </HeaderedItemsControl>
        </Border>
    </Grid>
</UserControl>
