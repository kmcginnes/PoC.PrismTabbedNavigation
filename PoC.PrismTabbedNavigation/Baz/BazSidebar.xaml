<UserControl x:Class="PoC.PrismTabbedNavigation.Baz.BazSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border BorderThickness="1" BorderBrush="Green">
            <HeaderedItemsControl Header="Baz" Style="{StaticResource SidebarHeaderedItemsControl}">
                <RadioButton GroupName="SidebarGroup" Content="Show Baz" Command="{Binding ShowBaz}"/>
            </HeaderedItemsControl>
        </Border>
    </Grid>
</UserControl>
